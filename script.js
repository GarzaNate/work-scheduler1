console.log('alive');

// Moment.js for calendar clock
var calendar = moment();
var today = moment();
$("#current-clock").text(calendar.format("MMM Do, YYYY"));

// console.log(calendar);
// $('#current-clock').text(calendar.toString());


// Time slot for each hour
// Color differences for recent, current, and future events
// Save changes to events to local storage
    // On click, be able to edit and save changes
    // Save button

